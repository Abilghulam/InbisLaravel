<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Kalau file/direktori tidak ada, arahkan ke folder public
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
